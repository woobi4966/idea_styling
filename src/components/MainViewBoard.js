import React, { useState } from 'react';
import css from './MainViewBoard.module.css';
import Text from './Text';

export default function MainViewBoard( ) {


    return(
        <div className={css.board}>
        </div>
    );
}